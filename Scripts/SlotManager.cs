using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Newtonsoft.Json.Linq;



namespace TalesPop.Items
{
    //public class SlotManager
    //{
    //    private readonly Dictionary<int, Slot> container;
    //    private readonly ItemManager itemManager;

    //    public SlotManager()
    //    {
    //        container = new Dictionary<int, Slot>();
    //        itemManager = new ItemManager();
    //    }

    //    public Slot CreateSlot(string json, int slotId, SlotType slotType)
    //    {
    //        Slot slot = new AnySlot(slotId, slotType)
    //        {
    //            item = itemManager.CreateItem(json)
    //        };

    //        if (slot.EnableInclude(slot.item.itemType))
    //        {

    //        }

    //        return slot;
    //    }
    //}
}
